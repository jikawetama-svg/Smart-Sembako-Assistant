<Window x:Class="SmartSembakoAssistant.Views.ProductSearchWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Cari Produk Aronium"
        Width="720"
        Height="520"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        Background="#F3F4F6">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="TxtSearch"
                     Grid.Column="0"
                     Padding="10,8"
                     FontSize="12"
                     TextChanged="TxtSearch_TextChanged"/>
            <Button x:Name="BtnRefresh"
                    Grid.Column="1"
                    Margin="8,0,0,0"
                    Padding="12,8"
                    Content="Refresh"
                    Click="BtnRefresh_Click"/>
        </Grid>

        <DataGrid x:Name="DgProducts"
                  Grid.Row="1"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  MouseDoubleClick="DgProducts_MouseDoubleClick">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="90"/>
                <DataGridTextColumn Header="Kode" Binding="{Binding Sku}" Width="130"/>
                <DataGridTextColumn Header="Nama Produk" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Unit" Binding="{Binding Unit}" Width="90"/>
                <DataGridTextColumn Header="Stok" Binding="{Binding Stock}" Width="90"/>
                <DataGridTextColumn Header="Status" Binding="{Binding ActiveStatus}" Width="90"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button x:Name="BtnCancel" Content="Batal" Padding="12,8" Margin="0,0,8,0" Click="BtnCancel_Click"/>
            <Button x:Name="BtnSelect" Content="Pilih" Padding="12,8" Click="BtnSelect_Click"/>
        </StackPanel>
    </Grid>
</Window>
